{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.edithatogo/osf-cli-go",
  "title": "OSF CLI Go",
  "description": "Read-only OSF and Zenodo discovery and metadata tools.",
  "version": "0.3.2",
  "repository": {
    "url": "https://github.com/edithatogo/osf-cli-go",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/edithatogo/osf-cli-go-osf-mcp:0.3.2",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "OSF_TOKEN",
          "description": "Preferred OSF personal access token.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "OSF_USERNAME",
          "description": "Optional OSF username fallback when OSF_TOKEN is not provided.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "OSF_PASSWORD",
          "description": "Optional OSF password fallback when OSF_TOKEN is not provided.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        }
      ]
    }
  ]
}

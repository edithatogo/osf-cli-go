version: "2"

run:
  timeout: 5m

formatters:
  enable:
    - gofmt
    - goimports

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
    - asciicheck
    - copyloopvar
    - decorder
    - errorlint
    - gocritic
    - gosec
    - nilerr
    - nolintlint
    - prealloc
    - predeclared
    - revive
    - thelper
    - tparallel
    - usetesting

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

// Package repository defines provider-qualified research repository contracts.
//
// It intentionally contains no network client interface. Shared behavior is
// promoted only when the concrete OSF and Zenodo descriptors can state its
// support level and provider-specific constraints without losing semantics.
package repository
